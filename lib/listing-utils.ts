// Membership / early-access placeholder logic was removed when JMM became
// free-membership + free-listings. This file is intentionally kept empty
// as a stub so any stale imports fail loudly at the import site.
export {}
